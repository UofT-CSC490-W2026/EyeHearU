"""
Evaluation utilities for the ASL classifier.

Most projects should use `ml/evaluation/evaluate.py` directly to:
  - Compute overall and per-class accuracy
  - Inspect top-5 accuracy
  - View the most common confusion pairs

This package file exists to make `ml.evaluation` importable if we later
decide to split metrics/plots into multiple modules.
"""